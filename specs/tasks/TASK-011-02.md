# TASK-011-02 — AppShell init + SettingsPage language switcher

**Status**: DONE
**Spec**: DES-011

## Implementation
- `AppShell.tsx` — on config load, calls `setLocale(config.general.language)` to sync locale
- `SettingsPage.tsx` — added Language section with flag+native-name buttons; clicking saves `config.general.language` and calls `setLocale()` immediately (no restart needed); all labels use `t()`
