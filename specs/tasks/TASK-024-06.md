---
id: TASK-024-06
title: "Update Tauri commands & PcImportGame model"
status: NOT_STARTED
des: DES-024
req: REQ-024
author: "architect"
created: 2026-04-28
updated: 2026-04-28
estimate: S
depends_on: [TASK-024-02, TASK-024-03, TASK-024-04, TASK-024-05]
tags: [rust, api, ipc]
---

# TASK-024-06: Update Tauri commands & PcImportGame model

## Context
Wires the four cloud service modules into the existing Tauri commands; adds `db: State<Database>`, `steam_id`, and `force_refresh` params; adds the new `clear_pc_cloud_cache` command; and extends `PcImportGame` with `installed: bool`.

## Parent Design
- **DES**: [DES-024 — Store Cloud Library Design](../design/DES-024-store-cloud-library.md)

## Acceptance Criteria
- [ ] `PcImportGame.installed: bool` field added (defaults to `true` for backward compat with local-scan)
- [ ] `import_steam_games(db, sgdb_key, steam_id, force_refresh)` signature updated
- [ ] `import_epic_games(db, sgdb_key, force_refresh)` signature updated
- [ ] `import_gog_games(db, sgdb_key, force_refresh)` signature updated
- [ ] `import_xbox_games(db, sgdb_key, force_refresh)` signature updated
- [ ] `clear_pc_cloud_cache(db, store: Option<String>)` command added and registered
- [ ] `AppConfig.general.steam_api_key: Option<String>` and `steam_id: Option<String>` added to `GeneralConfig`
- [ ] All new commands registered in `lib.rs` `generate_handler![]`

## Implementation Notes

### Files to Create/Modify
| File | Action | Description |
|------|--------|-------------|
| `src-tauri/src/services/pc_import_service.rs` | MODIFY | Add `installed: bool` to `PcImportGame`; default `true` in all existing scan paths |
| `src-tauri/src/commands/pc_games.rs` | MODIFY | Update command signatures, add `clear_pc_cloud_cache` |
| `src-tauri/src/lib.rs` | MODIFY | Register `clear_pc_cloud_cache` in `generate_handler!` |
| `src-tauri/src/models/config.rs` | MODIFY | Add `steam_api_key`, `steam_id` to `GeneralConfig` with `#[serde(default)]` |

### Backward compat
Local-scan paths in `import_steam`, `import_epic`, `import_gog`, `import_xbox` must all default `installed: true` so the existing save-to-library flow is unaffected.

## Test Plan
| Test | Type | Description |
|------|------|-------------|
| `test_local_scan_installed_true` | Unit | Local-scan result has installed=true |
| `test_clear_cache_command` | Integration | Calling clear empties the correct store's rows |

## Definition of Done
- [ ] All acceptance criteria pass
- [ ] `cargo check` clean
- [ ] Committed: `feat(pc): extend PcImportGame with installed flag + update commands [TASK-024-06]`
