---
id: TASK-015-01
title: "Database schema + CRUD for ROM path overrides"
status: DONE
design: DES-015
author: "copilot"
created: 2026-03-29
completed: 2026-03-29
tags: [database, backend]
---

# TASK-015-01: Database Schema + CRUD for ROM Path Overrides

## Context
Add the `system_rom_paths` table to the SQLite schema and implement CRUD methods on the `Database` struct.

## Acceptance Criteria
- [x] `system_rom_paths` table created in `schema.rs` with `system_id PRIMARY KEY`, `custom_path TEXT NOT NULL`, `created_at DATETIME DEFAULT CURRENT_TIMESTAMP`
- [x] `get_rom_path_overrides()` returns `HashMap<String, String>`
- [x] `set_rom_path_override(system_id, custom_path)` uses `INSERT OR REPLACE`
- [x] `delete_rom_path_override(system_id)` removes by primary key
- [x] Table is auto-created in `Database::new()` via `initialize_db()`

## Implementation Notes

### Files Modified
- `src-tauri/src/db/schema.rs` — Added `CREATE TABLE IF NOT EXISTS system_rom_paths` SQL
- `src-tauri/src/db/mod.rs` — Added 3 public methods to `Database` impl block

## Test Plan
- Verify table creation on fresh DB
- Insert, read, update, delete cycle for an override
- Ensure duplicate system_id replaces rather than errors

## Definition of Done
- [x] Code compiles with `cargo check`
- [x] CRUD operations work end-to-end via Tauri commands (tested in TASK-015-02)

## Completion Log
- Implemented `system_rom_paths` table creation in DB init path
- Added `get_rom_path_overrides()` with `query_map` returning `HashMap<String, String>`
- Added `set_rom_path_override()` with `INSERT OR REPLACE INTO` SQL
- Added `delete_rom_path_override()` with parametrized DELETE
- All methods use `rusqlite::params![]` macro; errors converted to `String` for IPC
