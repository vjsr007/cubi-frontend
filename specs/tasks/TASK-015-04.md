---
id: TASK-015-04
title: "Frontend RomPathsPage UI + navigation"
status: DONE
design: DES-015
author: "copilot"
created: 2026-03-29
completed: 2026-03-29
tags: [frontend, ui, settings]
---

# TASK-015-04: Frontend RomPathsPage UI + Navigation Integration

## Context
Build a dedicated React page for managing per-system ROM path overrides and integrate it into the navigation flow from the Settings page.

## Acceptance Criteria
- [x] `RomPathsPage.tsx` created with full CRUD UI
- [x] Displays all 21 registered systems with name, current path, and status badge
- [x] DEFAULT badge (grey) for systems using standard path; CUSTOM badge (green) for overrides
- [x] Inline editing: text input + "Browse" folder dialog + Save/Cancel
- [x] "Reset" button on custom paths removes the override
- [x] Search/filter input to narrow the system list
- [x] Info box explaining how overrides work
- [x] Back button returns to Settings page
- [x] "📁 ROM Paths" button on Settings page navigates to `rom-paths`
- [x] `rom-paths` page registered in both Default and HyperSpin themes
- [x] TypeScript types added: `SystemDefInfo`, `RomPathOverride`, `Page` union updated
- [x] API wrappers in `invoke.ts` for all 4 commands

## Implementation Notes

### Files Created
- `src/pages/RomPathsPage.tsx` (~300 lines)

### Files Modified
- `src/types/index.ts` — Added interfaces + page type
- `src/lib/invoke.ts` — Added 4 API wrapper functions
- `src/pages/SettingsPage.tsx` — Added navigation button
- `src/themes/default/index.tsx` — Registered page
- `src/themes/hyperspin/index.tsx` — Registered page (lazy import)

### UI Details
- Uses `@tauri-apps/plugin-dialog` `open()` for native folder browser
- Fetches system list + overrides on mount via `useEffect`
- Local state for editing mode per system (editingSystemId, editPath)
- Filter state filters by system name (case-insensitive)
- Tailwind CSS utility classes for all styling
- Responsive grid layout

## Test Plan
- Verify page loads and shows all systems
- Edit a path → Save → verify badge changes to CUSTOM
- Reset a custom path → verify badge changes to DEFAULT
- Filter systems by name
- Browse folder dialog opens native OS picker

## Definition of Done
- [x] Page renders correctly in both themes
- [x] CRUD operations work end-to-end
- [x] No TypeScript compilation errors
- [x] Navigation flow: Settings → ROM Paths → Back to Settings

## Completion Log
- Created `RomPathsPage.tsx` with complete state management (systems, overrides, editing, filter)
- Added `SystemDefInfo` and `RomPathOverride` to `types/index.ts`
- Added `'rom-paths'` to the `Page` union type
- Added 4 invoke wrappers: `getSystemRegistryList`, `getRomPathOverrides`, `setRomPathOverride`, `deleteRomPathOverride`
- Settings page: added "📁 ROM Paths" button in the settings grid
- Default theme: direct import and page registration
- HyperSpin theme: lazy import and page registration
- Tested in dev with `cargo tauri dev` — page loads, systems display, editing works
