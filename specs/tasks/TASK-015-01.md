# TASK-015-01 — DB Schema Migration: New PC Metadata Columns

## Status
`TODO`

## Linked Design
DES-015 — PC Game Enhanced Metadata & Web Scraper

## Description
Add new nullable columns to the `games` SQLite table and implement a versioned migration system. All new fields for enhanced PC metadata must be persisted.

## Acceptance Criteria
- [ ] `schema_version` table created if not exists (tracks migration version)
- [ ] Migration v2 adds columns: `hero_art`, `logo`, `background_art`, `screenshots` (TEXT, JSON array), `trailer_url`, `trailer_local`, `metacritic_score` (INTEGER), `tags` (TEXT, JSON array), `website`, `pcgamingwiki_url`, `igdb_id` (INTEGER)
- [ ] Migration runs on app startup via `db::migrations::run_migrations(conn)`
- [ ] Existing rows unaffected (all new columns nullable with no default)
- [ ] `GameInfo` Rust struct updated with new optional fields
- [ ] DB get/insert/update functions updated to read/write new columns
- [ ] `screenshots` and `tags` serialized as JSON arrays in TEXT column

## Files to Create/Modify
- `src-tauri/src/db/migrations.rs` (new)
- `src-tauri/src/db/mod.rs` — call `run_migrations` on init
- `src-tauri/src/models/game.rs` — add new fields to `GameInfo`
