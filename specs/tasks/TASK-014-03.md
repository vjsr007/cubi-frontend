# TASK-014-03 — Create release-manager Agent

## Status
`DONE`

## Linked Design
DES-014 — Release Management & CI/CD Build System

## Description
Create `.claude/agents/release-manager.md` — a Claude sub-agent that orchestrates the full release process: version validation, bumping, committing, tagging, and pushing.

## Acceptance Criteria
- [ ] File exists at `.claude/agents/release-manager.md`
- [ ] Agent reads current version from `src-tauri/tauri.conf.json`
- [ ] Agent validates all three version files are in sync before proceeding
- [ ] Agent proposes next version based on semver bump type
- [ ] Agent performs atomic edits across all three files
- [ ] Agent creates commit and tag with correct message format
- [ ] Agent requires explicit user confirmation before `git push`
- [ ] Agent aborts if working tree has uncommitted changes
- [ ] Agent outputs the expected GitHub Release URL after pushing
