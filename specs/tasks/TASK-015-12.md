# TASK-015-12 — TypeScript Types Extension

## Status
`TODO`

## Linked Design
DES-015 § 3.11

## Description
Extend TypeScript types in `src/types/index.ts` and add Tauri invoke wrappers for the new PC scraper commands.

## Acceptance Criteria
- [ ] `GameInfo` interface extended with new optional fields:
  `hero_art`, `logo`, `background_art`, `screenshots`, `trailer_url`, `trailer_local`, `metacritic_score`, `tags`, `website`, `pcgamingwiki_url`, `igdb_id`
- [ ] New interfaces added:
  - `PcScrapeJob { game_ids?: string[], sources: PcMetadataSource[], filter: ScrapeFilter, overwrite: boolean }`
  - `PcMetadataConfig { igdb_client_id?, igdb_client_secret?, steamgriddb_api_key?, mobygames_api_key?, youtube_api_key?, max_screenshots: number, download_trailers: boolean, use_headless_browser: boolean, enabled_sources: PcMetadataSource[] }`
  - `PcToolsStatus { ytdlp_found: boolean, ytdlp_path?: string, chrome_found: boolean, chrome_path?: string }`
  - `PcMetadataSource` union type: `'steam_store' | 'igdb' | 'steamgriddb' | 'mobygames' | 'pcgamingwiki' | 'youtube' | 'web_scraper'`
- [ ] `src/lib/invoke.ts` extended with new API calls:
  - `api.runPcMetadataJob(job: PcScrapeJob): Promise<ScrapeResult>`
  - `api.scrapeSinglePcGame(gameId: string, config: PcMetadataConfig): Promise<GameInfo>`
  - `api.checkPcScraperTools(): Promise<PcToolsStatus>`
  - `api.getPcMetadataConfig(): Promise<PcMetadataConfig>`
  - `api.savePcMetadataConfig(config: PcMetadataConfig): Promise<void>`

## Files to Modify
- `src/types/index.ts`
- `src/lib/invoke.ts`
