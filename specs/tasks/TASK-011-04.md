# TASK-011-04 — GameDetailPage translate/revert button

**Status**: DONE
**Spec**: DES-011

## Implementation
- `GameDetailPage.tsx` — uses `useTranslate()` hook; shows "🌐 Translate" button when locale ≠ en and no translation cached yet; shows spinner + "Translating..." while loading; after translation shows "📄 Show Original" toggle button; all metadata labels use `t()`; `getField()` returns translated or original transparently
