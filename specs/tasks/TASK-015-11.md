# TASK-015-11 — Tauri Commands: PC Scraper

## Status
`TODO`

## Linked Design
DES-015 § 3.9

## Description
Create Tauri IPC commands for the PC metadata pipeline and register them in `lib.rs`.

## Acceptance Criteria
- [ ] `run_pc_metadata_job(job: PcScrapeJob, app: AppHandle) -> Result<ScrapeResult, String>`
- [ ] `scrape_single_pc_game(game_id: String, config: PcMetadataConfig, app: AppHandle) -> Result<GameInfo, String>`
- [ ] `check_pc_scraper_tools() -> Result<PcToolsStatus, String>`
  - `PcToolsStatus { ytdlp_found: bool, ytdlp_path: Option<String>, chrome_found: bool, chrome_path: Option<String> }`
- [ ] `get_pc_metadata_config(app: AppHandle) -> Result<PcMetadataConfig, String>` — reads from TOML config
- [ ] `save_pc_metadata_config(config: PcMetadataConfig, app: AppHandle) -> Result<(), String>` — writes to TOML config
- [ ] All types derive `Serialize`, `Deserialize`, `Clone`
- [ ] All commands registered in `lib.rs` `generate_handler![]`
- [ ] `PcMetadataConfig` persisted in existing app TOML config file (new `[pc_metadata]` section)

## Files to Create
- `src-tauri/src/commands/pc_scraper.rs`

## Files to Modify
- `src-tauri/src/lib.rs` — register new commands
- `src-tauri/src/models/config.rs` — add `PcMetadataConfig` to app config struct
