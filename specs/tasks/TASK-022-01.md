---
id: TASK-022-01
title: "Database schema & models"
status: DONE
des: DES-022
req: REQ-022
author: implementor
created: 2026-04-01
updated: 2026-04-01
estimate: M
depends_on: []
tags: [rust, db]
---

# TASK-022-01: Database schema & models

## Context
Create the SQLite schema migration (v5) for catalog tables and the Rust data model structs that represent catalog games, sync metadata, stats, filters, pagination, and config.

## Parent Design
- **DES**: [DES-022 — Game Catalog Database](../design/DES-022-game-catalog-database.md)

## Acceptance Criteria
- [x] `CatalogGame` struct with all fields (id, system_id, title, region, sha1, md5, crc32, file_size, file_name, dat_name, owned, owned_game_id)
- [x] `CatalogSync` struct for DAT sync metadata
- [x] `CatalogSystemStats` struct for per-system aggregate stats
- [x] `CatalogPage` struct for paginated query results
- [x] `CatalogFilter` struct for query parameters
- [x] `CatalogConfig` struct with serde defaults, added to `AppConfig`
- [x] `MIGRATION_V5` in `schema.rs` creates `catalog_games` and `catalog_sync` tables with indexes
- [x] Migration runner in `db/mod.rs` applies v5

## Implementation Notes

### Files to Create/Modify
| File | Action | Description |
|------|--------|-------------|
| `src-tauri/src/models/catalog.rs` | CREATE | All catalog data structs |
| `src-tauri/src/models/mod.rs` | MODIFY | Add `pub mod catalog` + `pub use catalog::*` |
| `src-tauri/src/models/config.rs` | MODIFY | Add `catalog: CatalogConfig` to `AppConfig` |
| `src-tauri/src/db/schema.rs` | MODIFY | Add `MIGRATION_V5` constant |
| `src-tauri/src/db/mod.rs` | MODIFY | Run migration v5 in `run_migrations()` |

## Definition of Done
- [x] All acceptance criteria pass
- [x] No compiler warnings
- [x] CLAUDE.md conventions followed

---

## Completion Log
**Completed**: 2026-04-01
**Files changed**:
- `src-tauri/src/models/catalog.rs` — Created with CatalogGame, CatalogSync, CatalogSystemStats, CatalogPage, CatalogFilter, CatalogConfig
- `src-tauri/src/models/mod.rs` — Added `pub mod catalog` + `pub use catalog::*`
- `src-tauri/src/models/config.rs` — Added `catalog: CatalogConfig` to AppConfig with Default impl
- `src-tauri/src/db/schema.rs` — Added MIGRATION_V5 (catalog_games + catalog_sync tables, 5 indexes)
- `src-tauri/src/db/mod.rs` — Added migration v5 runner after v4 block
