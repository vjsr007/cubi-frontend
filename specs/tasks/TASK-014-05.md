# TASK-014-05 — Create /release Slash Command

## Status
`DONE`

## Linked Design
DES-014 — Release Management & CI/CD Build System

## Description
Create `.claude/commands/release.md` — a slash command that users can invoke with `/release` to start the guided release workflow via the release-manager agent.

## Acceptance Criteria
- [ ] File exists at `.claude/commands/release.md`
- [ ] Command invokes the release-manager agent
- [ ] Prompts user for bump type (patch/minor/major) or explicit version
- [ ] Shows current version before asking for new version
- [ ] Summarizes the steps that will be taken before executing
- [ ] Requires confirmation before push step
