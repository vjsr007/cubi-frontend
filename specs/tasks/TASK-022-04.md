---
id: TASK-022-04
title: "Tauri commands + config integration"
status: DONE
des: DES-022
req: REQ-022
author: implementor
created: 2026-04-01
updated: 2026-04-01
estimate: M
depends_on: [TASK-022-03]
tags: [rust, api]
---

# TASK-022-04: Tauri commands + config integration

## Context
Create the Tauri IPC command layer that exposes catalog service functions to the frontend. Register all commands in lib.rs.

## Parent Design
- **DES**: [DES-022 — Game Catalog Database](../design/DES-022-game-catalog-database.md)

## Acceptance Criteria
- [x] `get_catalog_stats` command returns `Vec<CatalogSystemStats>`
- [x] `get_catalog_games` command accepts `CatalogFilter`, returns `CatalogPage`
- [x] `import_dat_file` command accepts system_id + file_path, returns `CatalogSync`
- [x] `sync_catalog` command downloads DAT from URL (explicit, config, or default), returns `CatalogSync`
- [x] `get_default_dat_urls` command returns `HashMap<String, String>`
- [x] `refresh_catalog_ownership` command re-runs matching, returns match count
- [x] `get_catalog_config` / `set_catalog_download_url` commands for config
- [x] All 8 commands registered in `lib.rs` generate_handler

### Files to Create/Modify
| File | Action | Description |
|------|--------|-------------|
| `src-tauri/src/commands/catalog.rs` | CREATE | 8 Tauri IPC commands |
| `src-tauri/src/commands/mod.rs` | MODIFY | Add `pub mod catalog` |
| `src-tauri/src/lib.rs` | MODIFY | Register all catalog commands |

## Definition of Done
- [x] All acceptance criteria pass
- [x] No compiler warnings
- [x] CLAUDE.md conventions followed

---

## Completion Log
**Completed**: 2026-04-01
**Files changed**:
- `src-tauri/src/commands/catalog.rs` — Created with 8 commands including async `sync_catalog`
- `src-tauri/src/commands/mod.rs` — Added `pub mod catalog`
- `src-tauri/src/lib.rs` — Registered all 8 catalog commands in `generate_handler![]`
