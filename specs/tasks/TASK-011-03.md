# TASK-011-03 — translationStore + useTranslate hook

**Status**: DONE
**Spec**: DES-011

## Implementation
- `src/stores/translationStore.ts` — cache keyed by `gameId → locale → field`, `showOriginal` toggle per gameId
- `src/hooks/useTranslate.ts` — calls MyMemory free API (`api.mymemory.translated.net`), translates description/genre/developer/publisher in parallel, caches results, exposes `getField()` which returns translated or original based on toggle state
